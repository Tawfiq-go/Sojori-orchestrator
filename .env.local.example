# Configuration locale pour le développement
# Copie ce fichier en .env.local et modifie selon tes besoins

# Mode développement sans authentification (⚠️ JAMAIS en production!)
# Décommenter pour désactiver l'auth frontend
# VITE_DISABLE_AUTH=true

# URL des APIs (obligatoire au BUILD si tu utilises `vite preview` / port 4174 sans rebuild)
# Sinon le bundle prod embarque le fallback https://dev.sojori.com
# VITE_API_URL=https://dev.sojori.com

# Cookies JWT : secure uniquement en HTTPS réel (http://127.0.0.1:4174 OK après fix cookieUtils)

# Dev token pour contourner CORS depuis localhost
VITE_DEV_TOKEN=your_dev_token_here
