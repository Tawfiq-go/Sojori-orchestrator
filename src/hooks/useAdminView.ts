/**
 * « La vue » (2026-09-03) : quel owner l'admin regarde, quelle sidebar il
 * affiche. Même contexte que la simulation PM historique, nom d'usage neuf.
 */
export { usePmSimulation as useAdminView } from '../context/PmSimulationContext';
