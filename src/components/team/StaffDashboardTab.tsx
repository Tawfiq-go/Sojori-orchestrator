/** Dashboard Staff — planning + gestion (legacy) */
import StaffDashboardPage from '../../features/staff/StaffDashboard.page';

export function StaffDashboardTab() {
  return <StaffDashboardPage />;
}
