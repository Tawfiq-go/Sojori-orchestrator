/**
 * @deprecated Prefer `usePageFullscreen` from `components/page-fullscreen`.
 * Alias conservé pour les onglets inbox existants.
 */
export { usePageFullscreen as useInboxFullscreen } from '../page-fullscreen/usePageFullscreen';
